{
  "routes": [
    {
      "method": "GET",
      "path": "/votos",
      "handler": "votos.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/votos/count",
      "handler": "votos.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/votos/:id",
      "handler": "votos.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/votos",
      "handler": "votos.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/votos/:id",
      "handler": "votos.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/votos/:id",
      "handler": "votos.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
